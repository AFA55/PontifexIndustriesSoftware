*.log
.DS_Store
.claude/worktrees/
.env
.env*.local
.env.development
.env.local
.env.production
.next/
node_modules/
# Capacitor static export stub — not a real build artifact
/out/

# Ruflo runtime data (logs, sessions, learned data — machine-specific)
.claude-flow/logs/
.claude-flow/sessions/
.claude-flow/data/
.claude/memory.db
.swarm/

# Agent/swarm tooling artifacts (machine-specific, not source) — Jun 22 2026 cleanup
.claude-flow/
.claire/
agentdb.rvf
agentdb.rvf.lock
ruvector.db

# TypeScript incremental build cache (regenerated on build)
tsconfig.tsbuildinfo

# Stray generated icon set — the real served PWA icons live in public/
/icons/
